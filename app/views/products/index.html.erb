<h1>Products#index</h1>

<% @products.each do |product| %>
  <ul> 
    <li><%= link_to product.name%></li>
    <p><%=product.description %></p>
    <p><%=product.price_in_cents%></p>
    <%=product.formatted_price%>
  </ul>
<% end %>
